# Copy to backend.hcl (gitignored) and fill in the values from `terraform
# output` in bootstrap/. Then: terraform init -backend-config=backend.hcl
bucket       = "auditflow-tfstate-changeme"
key          = "staging/terraform.tfstate"
region       = "us-east-1"
use_lockfile = true
encrypt      = true
